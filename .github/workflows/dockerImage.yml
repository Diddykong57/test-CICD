name: Docker
run-name: ${{ github.actor }} is creating a Docker image 🚀

on:
  push:
    branches:
      - master

jobs:
#  build:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Starting
#        run: echo "💡 Start of the job 💡"
#
#      - name: pre-checkout
#        run: pwd
#
#      - name: Checkout du code
#        uses: actions/checkout@v4
#
#      - name: post-checkout
#        run: pwd
#
##      - name: Docker compose
##        run: docker compose up
#
#      - name: Image created
#        run: echo "🎉 The image was automatically created by ${{ github.actor }}."
#

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Starting
        run: echo "💡 Start of the job 💡"

      - name: Checkout du code
        uses: actions/checkout@v4

      - run: export
      - name: Log in to Docker registry
        run: docker login -u ${{ secrets.DOCKER_LOGIN }} -p ${{ secrets.DOCKER_PASSWORD }} https://hub.docker.com

      - name: Build de l'image Docker
        run: docker build -t votre_nom_dimage:latest .

      - name: Push de l'image Docker
        run: docker push votre_nom_dimage:latest

