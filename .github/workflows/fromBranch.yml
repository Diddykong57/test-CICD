name: From a branch

on:
  push:
    branches: 
      - '/features/**'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout du code
        uses: actions/checkout@v2

      - name: Exécuter des actions supplémentaires
        run: pwd

